# Python packages required by the skills.
#
# System tooling (nlm, libreoffice, ocrmypdf, poppler-utils, ghostscript,
# ffmpeg) is not installable from PyPI -- see the Prerequisites section of
# README.md, or run:
#   python3 skills/universal-transcriber/scripts/run_transcription.py --doctor

# transcriber-anki: builds native .apkg decks. Without it only .tsv export works.
genanki>=0.13,<1.0

# universal-transcriber: renders plain-text question banks to PDF before
# upload, since NotebookLM will not accept a .txt source.
reportlab>=4.0,<5.0

# Optional. Nothing here is needed to transcribe; each one unlocks one export
# format and the tool says which to install when you ask for that format.

# universal-transcriber: --question-bank --format xlsx
openpyxl>=3.1,<4.0

# universal-transcriber: --exam --format docx
python-docx>=1.1,<2.0
